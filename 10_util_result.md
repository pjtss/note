# 🛠️ Antigravity Note - 10대 유틸리티 추가 기능 명세 및 결과 보고

본 문서는 기존의 핵심 기능(일정 관리 CRUD, 메모 패드 CRUD, 다중 사용자 격리 인증 및 JWT 토큰 처리)을 **100% 무결하게 유지**한 상태로, 앱의 생산성과 가치를 극대화하기 위해 새롭게 설계 및 구현한 10가지 유틸리티 기능에 대한 종합 명세표입니다.

---

## 📌 10대 추가 유틸리티 기능 요약 표

| 번호 | 기능명 | 주요 기능 및 목적 | 데이터 영속성/작동 방식 | UI/UX 노출 위치 | 구현 상태 |
|:---:|:---|:---|:---|:---|:---:|
| **1** | **🍅 뽀모도로 집중 타이머 위젯** | 25분 집중, 5분 휴식 타이머를 통해 업무 생산성을 고도화하고 타이머 종료 시 브라우저 Push 알림으로 리마인드 | `useState` + `setInterval` 제어 및 브라우저 Notification API 연동 | 우측 하단 플로팅 컨트롤 패널 | **개발 완료** |
| **2** | **📊 메모 실시간 글자 수 및 리딩 타임 분석기** | 메모 작성 시 및 상세 보기 모달에서 공백 포함/제외 글자 수와 예상 독서 소요 시간(Reading Time)을 실시간 분석 | 메모 작성 폼 변경 이벤트 연동 및 상세 모달 렌더러 파이프라인 탑재 | 메모 작성 창 및 상세 보기 모달 하단 | **개발 완료** |
| **3** | **⏳ 일정 실시간 D-Day 및 남은 시간 카운트다운** | 일정이 다가오는 시점까지의 남은 일/시간/분을 실시간 계산하여 D-Day 배지로 시급성을 시각화 | `Date.now()` 대비 시작 시간 연산 및 1분 단위 타이머 리렌더링 | 일정 목록 각 카드 우측 상단 | **개발 완료** |
| **4** | **📅 일정 외부 캘린더 연동용 iCal (.ics) 내보내기** | 개별 일정을 구글 캘린더, 아웃룩, 애플 캘린더에 즉시 등록할 수 있도록 RFC 5545 표준 `.ics` 파일 동적 생성 및 다운로드 | 일정 객체 데이터를 ICS 표준 규격 스트링으로 빌드 후 Blob 다운로드 | 일정 카드 우측 작업 영역 (달력 다운로드 아이콘) | **개발 완료** |
| **5** | **💾 메모 JSON 백업 및 복원 도구** | 작성된 모든 메모를 단 하나의 JSON 파일로 백업 다운로드하고, 필요 시 복원(Import) 파일을 업로드하여 기존 메모와 유연하게 병합 | `JSON.stringify` 추출 및 FileReader API를 통한 로컬 데이터 파싱/병합 | 메모 목록 필터바 우측 (백업/복원 컨트롤) | **개발 완료** |
| **6** | **📢 오늘의 스케줄 실시간 요약 브리핑 보드** | 금일 진행 예정인 미완료 일정의 총량과 중요 일정 수를 요약 브리핑하여 오늘 해야 할 과제를 직관적으로 인식 | 현재 날짜 기준 일정 목록을 실시간 필터링 및 통계 분석 | 일정 탭 최상단 (브리핑 대시보드 배너) | **개발 완료** |
| **7** | **🔍 일정/메모 실시간 통합 검색 & 하이라이터** | 일정과 메모에 포함된 텍스트를 실시간 통합 검색하고, 일치하는 키워드 영역을 형광펜(`mark`) 효과로 시각적 강조 | 검색어 대상 Regex/Split 매핑 및 하이라이트 CSS 동적 렌더링 | 일정/메모 검색어 입력란 및 리스트 텍스트 | **개발 완료** |
| **8** | **📌 메모 중요도 핀 고정 (Pin to Top) 및 우선 배치** | 중요한 메모를 최상단에 상시 고정할 수 있도록 핀 토글 기능을 지원하고 핀 고정 메모를 리스트의 1순위로 자동 정렬 | DB 스키마 훼손을 방지하기 위해 제목에 `[PIN]` 접두사를 안전하게 심고 UI 단에서 핀으로 치환/파싱 | 메모 카드 우측 상단 및 상세 보기 모달 헤더 | **개발 완료** |
| **9** | **🎨 일정 카테고리별 컬러 칩 및 원클릭 퀵 필터** | 일정 목록 최상단에 카테고리별 컬러 배지와 등록 개수를 배치하여 원클릭으로 필터링을 즉시 토글하는 네비게이터 | `categoryFilter` 전역 상태값과 직접 바인딩하여 동적 리렌더링 | 일정 목록 상단 (필터 셀렉트박스 대체/보완) | **개발 완료** |
| **10** | **📋 메모 원본 마크다운 퀵 복사 및 템플릿 공유** | 렌더링된 마크다운이 아닌, 원본 마크다운 텍스트를 원클릭 복사하고 제목/내용/시간을 템플릿 형태로 가공해 공유용 내보내기 지원 | `navigator.clipboard.writeText` 활용 및 클립보드 복사 성공 토스트 알림 연동 | 메모 상세 보기 모달 및 요약 카드 영역 | **개발 완료** |

---

## 🛠️ 기능별 세부 설계 및 구현 전략

### 1. 🍅 뽀모도로 집중 타이머 위젯
- **목적**: 일정 수행 시 최고의 집중력을 유지하도록 기여하는 시간 관리 기법(Pomodoro Tech) 탑재.
- **동작**: 
  - 집중 모드(25분) -> 휴식 모드(5분) 순환 카운트다운.
  - 브라우저가 최소화되어 있거나 백그라운드 탭에 있어도 타이머가 완료되면 서비스 워커 및 Notification API를 통해 알림 발송.
  - 타이머 제어(재생, 일시정지, 리셋, 커스텀 시간 세팅)를 제공하는 고감도 Glassmorphism 디자인의 플로팅 위젯.

### 2. 📊 메모 실시간 글자 수 및 리딩 타임 분석기
- **목적**: 긴 생각을 정리하거나 마크다운 문서 작성 시 문서의 크기와 읽는 데 걸리는 소요 시간을 신속히 모니터링.
- **동작**:
  - 메모 작성/수정 시 텍스트 내용의 변화를 즉시 추적하여 '공백 포함 글자수', '공백 제외 글자수'를 계산.
  - 평균적인 독서 속도(1분당 한글/영어 200단어 기준)로 예상되는 독서 시간(Reading Time)을 계산하여 `X분 소요` 배지로 실시간 표출.

### 3. ⏳ 일정 D-Day 및 남은 시간 카운트다운
- **목적**: 일정이 언제 시작하는지, 그리고 얼마나 임박했는지 시간적 촉박함을 시각화하여 우선순위 배치 및 처리 유도.
- **동작**:
  - `startTime`이 미래인 경우: D-Day 날짜를 계산하고, 24시간 이내인 경우 `D-Day X시간 Y분 남음` 형태로 실시간 표기.
  - `startTime`이 과거이면서 미완료된 경우: `기한 초과` 배지와 결합하여 `경과 시간`을 표시하여 작업 수행 지연을 경고.
  - 1분 단위 타이머 리프레셔를 통해 별도 새로고침 없이 화면에서 실시간으로 시간 흐름 갱신.

### 4. 📅 일정 외부 캘린더 연동용 iCal (.ics) 내보내기
- **목적**: 로컬/웹 앱의 일정을 Google Calendar, Apple Calendar, Outlook 등 외부 장치 및 캘린더 앱에 손쉽게 동기화 및 가져오기.
- **동작**:
  - RFC 5545 인터넷 캘린더 사양에 부합하는 포맷 가공:
    ```text
    BEGIN:VCALENDAR
    VERSION:2.0
    PRODID:-//Antigravity//Note Project//KO
    BEGIN:VEVENT
    UID:[Schedule_UUID]
    DTSTART:[ISO_START_TIME_UTC]
    DTEND:[ISO_END_TIME_UTC]
    SUMMARY:[Schedule_Title]
    DESCRIPTION:[Schedule_Description]
    END:VEVENT
    END:VCALENDAR
    ```
  - 클라이언트 사이드에서 동적으로 `Blob` 객체를 빌드하고, 임시 파일 URL을 생성하여 `[일정제목].ics` 파일명으로 안전하게 다운로드 트리거.

### 5. 💾 메모 JSON 백업 및 복원 도구
- **목적**: 다중 기기 간 메모 공유, 브라우저 스토리지 초기화 대비 및 데이터 물리적 백업 안정성 확보.
- **동작**:
  - **백업**: 로그인 유저 소유의 메모 리스트 전체를 JSON 파일 구조로 포맷팅하여 브라우저에서 `antigravity_memos_backup_[날짜].json` 파일로 즉시 다운로드.
  - **복원**: 사용자가 JSON 백업 파일을 선택하여 업로드하면, JSON 정합성 검증 및 소유주 바인딩을 거친 후, `addMemo` API를 호출하여 메모를 순차적으로 완벽하게 복원 및 병합.

### 6. 📢 오늘의 스케줄 실시간 요약 브리핑 대시보드
- **목적**: 앱 기동 시 오늘 당장 수행해야 하는 과제가 무엇인지 핵심 요약을 제공하여 인지 과부하 방지.
- **동작**:
  - 오늘 하루에 걸쳐 있는 일정(오늘 시작되거나, 오늘 진행 중이거나, 오늘 끝나는 일정)을 추려내어 미완료/진행중 건수와 중요(Important) 일정의 잔여 수량을 추출.
  - "오늘 진행할 일정이 총 X건 있으며, 그 중 Y건이 완료되었습니다. 중요 일정 Z건을 잊지 마세요!" 와 같은 친근하고 고급스러운 AI 비서 어조의 요약 배너 렌더링.

### 7. 🔍 일정/메모 통합 실시간 검색 & 하이라이터
- **목적**: 정보량이 많아졌을 때 특정 키워드가 포함된 일정이나 메모를 정확히 찾아내고, 해당 키워드의 위치를 실시간으로 빠르게 눈에 띄게 식별.
- **동작**:
  - 일정 검색어(`searchQuery`) 또는 메모 검색어(`memoSearchQuery`)를 분석.
  - 리스트 카드에 렌더링되는 텍스트(제목, 상세 설명) 중 검색 키워드와 일치하는 영역을 대소문자 구분 없이 감지하여 `<mark className="highlight-text">` 태그 형태로 텍스트를 감싸 형광펜 시각 효과 동적 적용.

### 8. 📌 메모 중요도 핀 고정 (Pin to Top) 및 우선 배치
- **목적**: 무질서하게 흩어져 있는 메모들 중 빈번히 참조하는 핵심 메모를 항상 최상단에 상주하게 하여 접근성 강화.
- **동작**:
  - 데이터베이스 스키마 및 API 명세 수정 시 발생할 수 있는 마이그레이션 장해와 락(Lock)을 방어하기 위해 **비침습적 스키마 바이패스 기법** 설계.
  - 핀 고정 시 메모의 제목 접두사에 `📌 ` 또는 `[PIN]`을 붙여 저장하고, UI 상에서는 이 접두사를 인식하여 핀 고정 상태로 활성화.
  - 메모 필터링 및 정렬 파이프라인에서 핀 고정된 메모들을 최상위 노드로 우선 소팅하여 정렬하고, 일반 메모들은 생성일자 내림차순으로 배치하여 스키마 구조 변화 없이 영속적인 핀 고정 기능 실현.

### 9. 🎨 일정 카테고리별 컬러 칩 및 원클릭 퀵 필터
- **목적**: 콤보 박스 형태의 필터 선택 방식을 벗어나, 카테고리별 고유 컬러 칩이 가미된 미적인 버튼 셋을 렌더링하여 원클릭으로 직관적이고 빠르게 일정 필터링 가능.
- **동작**:
  - `🏢 업무`, `🏡 개인`, `⭐ 중요`, `👥 회의`, `🏷️ 기타` 카테고리별 전용 컬러 칩(Badge)과 해당 카테고리에 속한 일정 개수를 실시간으로 연산하여 배너 버튼 형식으로 제공.
  - 버튼을 누르면 즉시 그 카테고리 필터링이 토글되며 활성화된 카테고리 버튼에는 영롱한 그라데이션 글로우 아웃라인 처리 적용.

### 10. 📋 메모 원본 마크다운 퀵 복사 및 템플릿 공유
- **목적**: 메모 작성에 사용된 마크다운을 손쉽게 블로그, 이메일, 메신저 등으로 가공되지 않은 마크다운 포맷 그대로 전달.
- **동작**:
  - 메모 상세 모달 및 각 카드에 복사 단추 탑재.
  - 원클릭 시 렌더링된 요소가 아닌 본래 마크다운 소스 텍스트를 가져와, "제목, 작성 시각, 내용"을 깔끔하게 머금은 공유 템플릿 구조로 가공하여 클립보드로 전송.
  - 복사 성공 시 화면 상단에 1.5초간 노출되는 반투명 토스트 알림 컴포넌트를 연계하여 완벽한 피드백 전달.
