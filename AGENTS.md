# AGENTS.md

이 파일은 에이전트의 모든 행동 지침 및 규칙을 정의하고 자체적으로 개선하기 위해 사용됩니다. 모든 규칙 변경 사항은 이 파일에 저장 및 갱신되어야 합니다.

## 📌 기본 행동 규칙

### 1. 개발 내용 기록 및 문서화
- 모든 개발 관련 내용, 진행 상황, 변경 이력, 기능 분석 등은 반드시 `Development.md` 파일에 기록하고 최신화해야 합니다.

### 2. 규칙 자체 관리 및 개선
- 본 파일(`AGENTS.md`)은 에이전트의 작동 규칙을 명문화하는 유일한 파일입니다.
- 새로운 규칙이 생기거나 기존 규칙을 수정해야 하는 경우, 이 파일에 즉각 반영하여 시스템을 지속적으로 개선해 나갑니다.

### 3. 작업 영역(Workspace) 준수 및 권한 존중
- 작업은 허용된 워크스페이스인 `c:\Users\dldbs\Desktop\note` 내에서 수행합니다.
- 워크스페이스 외부 파일에 대한 접근이나 권한이 없는 파일 조작 요구는 정중히 거절하거나 사용자의 명시적 허가를 받아서 진행합니다.

### 4. 의사소통 스타일 및 톤앤매너
- 과장되거나 극단적인 표현(예: "완벽히 해결", "100% 검증" 등)을 피하고 겸손하고 전문적인 객관적 어조를 유지합니다.
- 작업 완료 후에는 사실에 기반하여 겸손하게 요약합니다.

---

## 🏗️ 테스트 코드 최적화 아키텍처 규칙

테스트하기 용이하고 유지보수가 쉬운 코드를 작성하기 위해 에이전트는 다음 설계 원칙을 준수합니다.

### 1. 테스트 용이성(Testability) 최우선 설계
- **순수 함수(Pure Function)의 격리**: 사이드 이펙트(API 요청, DB 쓰기, 현재 시간 의존성 등)가 포함된 코드와 순수 비즈니스 로직(연산, 변환 등)을 분리합니다. 순수 비즈니스 로직은 외부 의존성 없이 독립적으로 단위 테스트가 가능해야 합니다.
- **의존성 주입(Dependency Injection, DI) 적극 활용**: 하드코딩된 인스턴스 생성(예: `new Service()`)을 피하고, 외부에서 의존성을 주입받아 테스트 중에 모의 객체(Mock/Stub)로 손쉽게 대체할 수 있도록 구현합니다.
- **시간 및 전역 상태 제어**: `Date.now()`, `Math.random()`, `window.location`과 같이 테스트 실행 시점에 따라 결과가 바뀌는 요소는 매개변수로 받거나 래퍼 클래스로 감싸서 테스트 제어력을 확보합니다.

### 2. 효율적인 테스트 아키텍처 구성
- **테스트 피라미드 준수**: 실행 비용이 낮고 빠른 **단위 테스트(Unit Test)**의 비중을 가장 높게 가져가고, 계층 간 결합을 검증하는 **통합 테스트(Integration Test)**, 최종 사용자 시나리오를 검증하는 **E2E 테스트**의 순서로 아키텍처를 계층화합니다.
- **Mocking 최소화 및 모듈 경계 설정**: 과도한 모킹은 테스트 코드를 쉽게 깨지게 만듭니다. 어댑터나 게이트웨이 계층과 같이 시스템 경계면에 한해서만 모킹을 사용하고, 내부 도메인 간 테스트는 실제 객체나 가짜 구현체(Fakes)를 사용하도록 유도합니다.

---

## 💻 웹 개발 프론트엔드/백엔드 최적화 규칙

프론트엔드 및 백엔드 개발 시 아키텍처의 안정성과 생산성을 향상하기 위한 세부 규칙입니다.

### 1. 프론트엔드 (Frontend) 규칙
- **UI와 비즈니스 로직의 격리**: React, Vue 등 현대적인 프레임워크 사용 시 비즈니스 로직(상태 관리, 데이터 변환 등)은 Custom Hooks나 별도의 상태 관리 계층으로 분리하고, 컴포넌트는 오직 데이터 렌더링과 UI 인터랙션만 담당하도록 분리합니다.
- **컴포넌트의 단일 책임 원칙 (SRP)**: 하나의 컴포넌트는 한 가지 주요 역할만 담당하도록 쪼개며, 재사용성과 확장성을 고려하여 Atomic Design 패러다임을 반영한 구조 설계를 지향합니다.
- **전역/로컬 상태 분리의 명확화**: 컴포넌트 내부에서 소유 가능한 상태는 가급적 로컬 상태로 유지하고, 불필요한 전역 상태 공유로 인한 렌더링 병목 현상을 방지합니다.

### 2. 백엔드 (Backend) 규칙
- **관심사 분리 (Separation of Concerns, SoC)**: 계층 아키텍처(Controller -> Service -> Repository / UseCase -> Entity)를 명확히 고수하며, 각 레이어의 책임 경계를 침범하지 않습니다. (예: Controller에서 직접 DB 쿼리를 수행하지 않음)
- **도메인 중심의 비즈니스 로직 격리**: 서비스 계층이나 비즈니스 도메인 엔티티는 프레임워크나 외부 데이터베이스 연동 기술(예: TypeORM, JPA 등)에 너무 깊게 결합하지 않도록 비즈니스 로직 핵심을 격리하여 독립성을 보장합니다.
- **예외 처리 및 API 표준 명세 준수**: 모든 예외는 에러 핸들러/미들웨어를 통해 중앙 집중식으로 균일하게 처리하며, 명확한 상태 코드와 표준화된 응답 포맷(JSON)을 제공합니다.

---

*본 규칙은 에이전트가 자체적으로 검토하고 필요시 능동적으로 개선해 나갑니다.*

